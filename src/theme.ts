import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primaryBlue: "#82a5d6",
    darkGray: "#8b8b8b",
    lightGray: "#b2bdbd",
  },
});

export default theme;
